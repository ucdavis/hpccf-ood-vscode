---
cluster: "franklin"
form:
  - modules
  - auto_accounts
  - auto_queues
  - cores
  - mem
  - gpu_num
  - gpu_type
  - extra_modules
  - bc_num_hours
  - path
  - bc_email_on_started

attributes:
  modules: "code-server"
  extra_modules:
    widget: text_area
    label: "Additional Modules"
    help: |
      If needed, enter any additional modules to be loaded separated by spaces (i.e cuda/11.7.1 nvhpc/23.1 )
  auto_accounts:
    help: |
      Select the slurm group account (i.e adamgrp)
  auto_queues:
    help: |
      Select a slurm partition. **We strongly recommend a high priority partition**
  cores:
    widget: "number_field"
    label: "Number of cores"
    value: 8
    min: 1
    max: 256
    help: |
      Enter the number of cores needed for your job
  mem:
    widget: "number_field"
    label: "Amount of memory"
    value: 32
    min: 4
    max: 4096
    help: |
      Enter the amount of RAM in GB you need for your job
  gpu_num:
    widget: "number_field"
    label: "Number of GPUs"
    value: 0
    min: 0
    max: 8
    help: |
      Enter the amount of GPUs needed for your job
  gpu_type:
    widget: "text_field"
    label: "Type of GPU(s)"
    help: |
      Enter a constraint on the GPU type (i.e. a5500).
      **Advanced use only**, do not use unless you know what you're doing. (optional)
  bc_num_hours:
    help: |
      Your job will be automatically killed after this amount of time
  path:
    widget: "path_selector"
    label: "Working Directory"
    value: <%= ENV['HOME'] %>
    show_hidden: true
    show_files: false
    favorites:
      - <%= ENV['HOME'] %>
      - /scratch
    help: |
      Select the directory wherein you want to open and use this app
